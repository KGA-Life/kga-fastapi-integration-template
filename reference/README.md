# reference/

Commit the provider's own API documentation here (PDFs, OpenAPI specs, exported HTML,
notes) for offline reference while building and maintaining this service.

**This directory is UNMANAGED** — it is per-service and is never touched by a config sync
(unlike `CLAUDE.md` and `.claude/`). Its contents are specific to the provider this repo
wraps.
